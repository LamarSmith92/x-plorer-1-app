<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container pull-left">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <%= link_to 'Home', root_path, class: 'navbar-brand' %>
      <% if !current_user %>
<%= link_to "Sign up", new_user_registration_path, class: 'navbar-brand'  %>
    <% end %>
      <% if current_user %>
      <%= link_to "Profile", user_path(current_user.id), class: 'navbar-brand'  %>

      <% end %>
      <% if current_user %>
      <%= link_to 'Logout', destroy_user_session_path, class: 'navbar-brand', :method => :delete, data: {:confirm=>'Are you sure?'} %>
<% else %>
<%= link_to "Login",  new_user_session_path, class: 'navbar-brand'  %>
<% end %>
      </div>
    </div>
    <div class="collapse navbar-collapse">

      <ul class="nav navbar-nav">

        <%= render 'layouts/navigation_links' %>

      </ul>

    </div>
  </div>

</nav>
